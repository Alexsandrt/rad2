spring:
  application:
    name: rad2
  datasource:
    url: jdbc:postgresql://db:5432/ct
    username: postgres
    password: qwert
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate.dialect: org.hibernate.dialect.PostgreSQLDialect
    show-sql: true

server:
  port: 8080